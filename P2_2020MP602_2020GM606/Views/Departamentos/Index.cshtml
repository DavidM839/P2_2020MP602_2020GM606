@model IEnumerable<P2_2020MP602_2020GM606.Models.Departamentos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_dep)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_dep)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_departamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_departamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_departamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
